function Navbar() {
    return (
        <nav className="navbar">
            <a href="#" className="active">Dashboard</a>
            <a href="#">Courses</a>
            <a href="#">Assignments</a>
            <a href="#">Progress</a>
            <a href="#">Profile</a>
        </nav>
    );
}

export default Navbar;